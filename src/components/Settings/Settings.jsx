import React from 'react';
import dialogs from './Settings.module.css'

const Settings = () => {
return(
    <div className={dialogs.mikufan}>
        Setuper
    </div>
    )
};

export default Settings;